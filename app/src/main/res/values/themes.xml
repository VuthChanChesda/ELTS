<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.ELTS" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue</item>
    </style>

    <!-- Splash theme -->
    <style name="Theme.ELTS.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ELTS</item> <!-- FIXED -->
    </style>



    <!-- Final app theme -->
    <style name="Theme.ELTS" parent="Base.Theme.ELTS" />
</resources>
