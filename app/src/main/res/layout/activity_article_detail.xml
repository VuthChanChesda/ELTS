<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/articleToolbar"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:elevation="4dp" />

    <!-- Scrollable Content -->
    <ScrollView
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="24dp"
        android:paddingBottom="32dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Date -->
            <TextView
                android:id="@+id/articleDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="August 6, 2025"
                android:textSize="14sp"
                android:textColor="#888888"
                android:fontFamily="@font/inter_regular" />

            <!-- Title -->
            <TextView
                android:id="@+id/articleTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Article Title"
                android:textStyle="bold"
                android:textSize="35sp"
                android:textColor="#111111"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="16dp"
                android:fontFamily="@font/inter_bold" />

            <!-- Description Section -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/article_description_bg"
                android:orientation="vertical"
                android:padding="16dp"
                android:layout_marginBottom="24dp">

                <TextView
                    android:id="@+id/highlightTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Highlights"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:textSize="18sp"
                    android:fontFamily="@font/inter_bold"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:id="@+id/articleDescription"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="If you want to study, work, or migrate to the UK, find out which IELTS test you need to take. Discover what it’s like to live in the United Kingdom, learn about its rich culture, prospering education system, and more."
                    android:textSize="16sp"
                    android:lineSpacingExtra="4dp"
                    android:textColor="@color/white"
                    android:fontFamily="@font/inter_regular" />
            </LinearLayout>

            <!-- Content -->
            <TextView
                android:id="@+id/articleContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Full article content goes here..."
                android:textSize="18sp"
                android:lineSpacingExtra="4dp"
                android:layout_marginBottom="32dp"
                android:textColor="#222222"
                android:fontFamily="@font/inter_regular" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
